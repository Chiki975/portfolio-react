import { Helmet } from "react-helmet";

const Contacto = () => {
  return (
    <div>
      <Helmet>
        <title>Contacto</title>
      </Helmet>
      <div>Contacto</div>
    </div>
  );
};

export default Contacto;
